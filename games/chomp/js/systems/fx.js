// systems/fx.js — particles, trails, float text, screen shake, hitstop
// (GDD "Juice checklist"). One pooled ParticleSystem per FX type (perf budget).

// TODO(MD-06): subscribe to events (eat, evolve, damage, death…) and spend juice.
export function createFx(scene) {
  // TODO(MD-06)
}
